#include "struct/datastruct.h"
#include "struct/state.h"
#include "io.h"

void main() {
    State test;
    Queue *testStack;

    readState(&test, "NH", 2020);

    //stack tests
    testStack = qu_init(NULL);

}
